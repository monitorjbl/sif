git:
  commitMessage: "Release of v${version}"
  requireCleanWorkingDir: false
github:
  release: false
  releaseName: "v${version}"
  assets:
    - dist/sif-*
hooks:
  after:git:release: "VERSION=${version} ./scripts/build.sh"